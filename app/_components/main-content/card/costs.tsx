import { Card } from "@/_components/main-content/card"
import { ItemStatus } from "@constant"

export const Costs = () => {
  return (
    <Card title='Costs' type={ItemStatus.Costs} withFooter>
      content
    </Card>
  )
}
